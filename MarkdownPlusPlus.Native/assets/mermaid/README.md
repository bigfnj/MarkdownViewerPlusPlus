# Mermaid Asset

Place the pinned build-time Mermaid bundle here as:

```text
mermaid.min.js
```

The native preview inlines this file when present so diagrams work offline. The selected release policy is "latest as of build, pinned per plugin release."
