# README screenshots

These images are referenced by the top-level `README.md`.

| File | Shows |
| --- | --- |
| `rich-markdown-preview.png` | The live preview panel rendering a GitHub-flavored Markdown document beside the Notepad++ editor. |
| `mermaid-diagram-preview.png` | The preview panel rendering a Mermaid flowchart from a fenced `mermaid` code block. |

Replace either file in place to update the README; keep the same filenames.
